import { createMuiTheme } from "@material-ui/core";

const notifications = createMuiTheme({
  typography: {
    fontSize: 13.5,
    fontFamily: 'Inter, sans-serif',
  }
});

export default notifications;
